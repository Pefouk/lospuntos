@include('main')

@pushTo('main')
  <div class="auth-form">
    <h1>Inscription</h1>

    @component('components/form', {
      formAction: route('auth.register.store'),
      formMethod: 'post',
      formClass: ''
    })
      @component('components/formfield', {
        name: 'username',
        label: 'Pseudo',
        type: 'text'
      })
      @end

      @component('components/formfield', {
        name: 'name',
        label: 'Prénom',
        type: 'text'
      })
      @end

      @component('components/formfield', {
        name: 'password',
        label: 'Mot de passe',
        type: 'password'
      })
      @end

      @component('components/formfield', {
        name: 'confirmPassword',
        label: 'Confirmer le mot de passe',
        type: 'password'
      })
      @end

      <input type="submit" value="Valider" class="btn btn-primary">
    @end
  </div>
@end
